#include <iomanip>
#include <iostream>
using namespace std;

// Declare the function prototype getinput()
// Declare the function prototype maxmin()

// complete the function getinput( )
// complete the function maxmin( )
